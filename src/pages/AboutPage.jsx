export default function AboutPage() {
  return (
    <section>
      <h2>About Red Suite</h2>
      <p>
        React Router powers this page, while a single shared layout keeps navigation and
        structure consistent across routes.
      </p>
    </section>
  );
}
